meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{booking_base}}/health
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
