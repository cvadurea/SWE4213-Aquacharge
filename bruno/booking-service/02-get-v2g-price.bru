meta {
  name: Get V2G Price
  type: http
  seq: 2
}

get {
  url: {{booking_base}}/v2g/price
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
