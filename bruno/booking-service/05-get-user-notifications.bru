meta {
  name: Get User Notifications
  type: http
  seq: 5
}

get {
  url: {{booking_base}}/notifications/user/1?unreadOnly=false&limit=5
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
