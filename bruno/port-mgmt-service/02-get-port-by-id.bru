meta {
  name: Get Port By Id
  type: http
  seq: 2
}

get {
  url: {{port_base}}/ports/1
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
