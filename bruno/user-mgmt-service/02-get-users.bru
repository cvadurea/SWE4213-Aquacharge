meta {
  name: Get Users
  type: http
  seq: 2
}

get {
  url: {{user_base}}/users
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
