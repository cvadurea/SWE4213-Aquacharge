meta {
  name: Get User By Id
  type: http
  seq: 3
}

get {
  url: {{user_base}}/users/1
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
